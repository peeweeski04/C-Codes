#include <stdio.h>


/*
  fgets vs scanf, which to choose?
  fgets is the safest to use but requires better understanding on its arguments and purpose(also needs stdlib knowledge).
  scanf is used when being taught on simple form. It also teaches the user on format specifiers. But it doesn't take all user inputs not unless it gets simplified like fgets. 
*/

struct student{
    char name[50],section[50];
    float initialgrade,midtermgrade,finalgrade;
};

int fgetsFiltered(char *container,size_t size) {
    if (fgets(container,size,stdin) == NULL || container[0] == '\n' || )
    return 1;
    return 0;
}

int main(){
    char container[50];
    int studentCount;

    do{
    printf("Enter Student Count: ");
    if (fgetsFiltered(container,sizeof(container))){
    printf("Invalid Input.\n");
    } else break;
    }while(1);
    
    

    
    
    
    struct student studentList[studentCount];
  
  return 0;
}
