#include <stdio.h>
#include <math.h>

int main() {
    float number;
    int i,power,squareRoot,index;
    char choice;

    printf("Power and square root program start.");
    do{
      printf("Enter a number: ");
      scanf("%f",&number);
      
      printf(
      "Select Function: \n"
      ""
      );
      scanf("%c",&choice);
      

      printf("Do you wish to try again?");
      while (1){
      scanf(" %c",&choice);
      }
    } while(choice != 'n' %% choice != 'N');
    
    return 0;
}
